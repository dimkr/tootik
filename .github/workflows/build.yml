name: build

on:
  workflow_call:
    inputs:
      version:
        type: string

jobs:
  analysis:
    permissions: {}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
      - run: |
          go mod tidy -compat=$(grep -m1 ^go go.mod | cut -f 2 -d ' ')
          go generate ./migrations
          go fix ./...
          git diff --exit-code
          go vet ./...
  static:
    permissions: {}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      matrix:
        goarch: [amd64, arm64, arm, 386]
    steps:
      - uses: actions/checkout@v6
      - run: echo ${{ matrix.goarch }} > goarch
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
          cache-dependency-path: |
            go.sum
            goarch
      - run: go generate ./migrations
      - run: |
          version="${{ inputs.version }}"
          echo "TOOTIK_VERSION=${version#v}" >> "$GITHUB_ENV"
      - run: CGO_ENABLED=0 GOARCH=${{ matrix.goarch }} go build -tags no_pprof -ldflags "-w -s -X github.com/dimkr/tootik/buildinfo.Version=${TOOTIK_VERSION}" -trimpath -o tootik-${{ matrix.goarch }} ./cmd/tootik
      - uses: actions/upload-artifact@v6
        with:
          name: tootik-${{ inputs.version }}-${{ matrix.goarch }}
          path: tootik-${{ matrix.goarch }}
          retention-days: 1
  test:
    permissions: {}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      matrix:
        include:
          - flags: "-tags sqlite_mattn,fts5"
            race: true
          - flags: "-tags sqlite_modernc"
            race: false
          - flags: "-tags sqlite_ncruces"
            race: false
    steps:
      - uses: actions/checkout@v6
      - run: echo "${{ matrix.flags }}" > flags
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
          cache-dependency-path: |
            go.sum
            flags
      - run: |
          go generate ./migrations
          go test ./... -failfast -vet off ${{ matrix.flags }} ${{ matrix.race && '-race' || '' }}
  demo:
    permissions: {}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
      - run: go generate ./... && TERM=xterm-256color CGO_ENABLED=0 go run ./cmd/demo -record demo.cast
      - run: go tool termsvg export demo.cast -o demo.svg -m
      - uses: actions/upload-artifact@v6
        with:
          name: demo-${{ inputs.version }}
          path: ./demo.svg
          retention-days: 1
